DATABASE_URL="postgres://..."
SESSION_SECRET="your-session-secret"
VITE_API_BASE_URL="http://localhost:5000"

# Optional: default date format preference for Excel import when client does not provide one
# Allowed values: DMY (day/month/year), MDY (month/day/year)
IMPORT_DATE_FORMAT=DMY

# The /api/chatbot/monthly-summary/:employeeId endpoint requires an
# authenticated session with role admin, hr, or employee.
