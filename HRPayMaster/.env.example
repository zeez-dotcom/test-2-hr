# ── Database (Neon) ────────────────────────────────────────────────────────────
DATABASE_URL=postgresql://neondb_owner:npg_pLvn0hokiqj2@ep-quiet-pond-admfsoga-pooler.c-2.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require

# ── App secrets ───────────────────────────────────────────────────────────────
# ⚠️ Change this to a long random string before going to production!
SESSION_SECRET=change-me-to-a-long-random-string
EMAIL_FROM=no-reply@example.com

# ── API server config ─────────────────────────────────────────────────────────
API_BASE_URL=http://localhost:5000
PORT=5000
PASSWORD_RESET_URL=http://localhost:5000

# ── Business logic / feature flags ────────────────────────────────────────────
# Default date format preference for Excel import when client does not provide one
# Allowed values: DMY (day/month/year), MDY (month/day/year)
IMPORT_DATE_FORMAT=DMY

# Notes:
# The /api/chatbot/monthly-summary/:employeeId endpoint requires an authenticated
# session with role admin, hr, or employee.
